{
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/server.js",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
